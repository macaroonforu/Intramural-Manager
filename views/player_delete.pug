extends layout

block content
  h1 !{title}

   p Do you really want to delete this Player? (!{player.name})
    form(method='POST' action='')
      input(type='hidden', name='playerid', required='true', value=player._id)
      button(type='submit') Delete 